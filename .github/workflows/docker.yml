name: Docker Build And Push To Docker Hub

env:
  APP_NAME: ${{ github.repository }}

on:
  push:
    branches: [ main ]

jobs:
  build:
    name: Build Docker image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        name: Check out code

      - name: npm install jinjia2-cli
        run: |
         echo ${{github.event.repository.name:7}}
         echo "ok"
